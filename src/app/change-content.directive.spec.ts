import { ChangeContentDirective } from './change-content.directive';

describe('ChangeContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeContentDirective();
    expect(directive).toBeTruthy();
  });
});
